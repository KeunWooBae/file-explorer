# 0.6.1 검증 기록

## 변경 사항

- 패치 버전 증가: 0.6.0 → 0.6.1.
- SSH/SFTP 메뉴 아이콘과 Follow terminal folder 선택·저장.
- 기본 .ssh 폴더 개인키 자동 식별 / 비밀번호의 두 가지 인증 방식.
- 성공한 연결의 호스트·포트·사용자·인증 방식·이름 저장 및 다시 선택·삭제.
- 저장 세션 기반 다중 SSH 터미널, 공통 명령, 터미널별 동시 명령 제외.
- 별도의 파일 전송 대상 선택과 호스트별 목적지 폴더 지정, 병렬 업로드 및 개별 결과 표시.

## 검증 결과

- 문법 검사 통과.
- 백엔드 44개 통과, 교차 드라이브 테스트 1개는 별도 드라이브 미설정으로 건너뜀.
- 실제 Bash의 프롬프트 훅에서 cd 결과를 감지. 원격 시작 설정 파일을 수정하지 않음.
- 분할된 경로 보고 스트림과 한글 경로, 잘못된 경로·다른 토큰 거부.
- 저장 세션의 동시 쓰기·중복 처리·다시 읽기·삭제 및 비밀정보 미저장.
- 실제 SSH/SFTP 서버에 두 연결을 만들고 양쪽 명령 전달, 한쪽만 전달, 서로 다른 두 폴더로 파일 전송 검증.
- 브라우저 UI: 저장 세션, 정확히 두 인증 방식, 두 메뉴 아이콘, 따라가기 켜기·끄기, 명령 제외와 파일 대상의 독립성, 화면 전환 확인.
- 실제 패키지 UI: SSH 연결, 폴더 보고에 따른 원격 파일 목록 이동, 세션 저장, 두 개의 SSH 터미널, 제외한 터미널에 명령 미전달, 각 원격 폴더로 실제 파일 업로드, 재로드 후 세션 및 글자 크기 유지. 콘솔 오류 없음.

## 범위

Follow terminal folder는 Bash/Zsh의 기본 SSH 셸을 대상으로 합니다. 중첩 SSH·tmux·다른 셸은 범위 밖입니다. 옵션을 바꿀 때 실행 중인 프로그램에 명령을 주입하지 않습니다.

키 인증은 기본 .ssh 폴더를 사용하며 외부 IdentityFile 및 에이전트는 사용하지 않습니다. 비밀번호와 키 암호는 저장하지 않습니다. 사용자의 실제 원격 서버로는 접속하지 않았습니다.
