# 0.6.0 검증 기록

## 구현 범위

- 0.5의 탐색기 확장 기능을 제거하고 0.4 기본 동작을 복원.
- 왼쪽 SSH/SFTP 메뉴, 연결 폼, 로컬·원격 파일과 실제 SSH PTY의 3열 화면.
- OpenSSH 설정·기본 키·에이전트 자동 인증, 기존 known_hosts 검사.
- 글자 크기 선택 및 저장, 터미널 크기 조절, 대량 출력 흐름 제어.
- 기존 두 작업 공간의 0.5 경로 설정을 0.4 형식으로 읽는 호환 처리.

## 수행한 검증

- 문법 검사 통과.
- 백엔드 테스트 40개 통과, 교차 드라이브 테스트 1개는 별도 대상 드라이브 미설정으로 건너뜀.
- 실제 루프백 SSH/SFTP 서버: 키 인증, 비밀번호 인증, 파일·폴더 업로드/다운로드, 덮어쓰기 방지, 서버 키 변경 차단, PTY 입력·출력과 창 크기 변경.
- 별칭의 IdentityFile·User·Port 인식, 해시 known_hosts와 폐기된 키, 기존 세션 경로 호환성.
- 브라우저 회귀 8개 그룹: 기본 탐색기 화면, 선택, 파일 작업, 드래그, 반응형 배치.
- 새 SSH UI: 연결 폼, 좌→우 배치, 업로드·다운로드 요청, 터미널 키보드 입력, 글자 크기 저장 및 화면 전환. 콘솔 오류 없음.
- 실제 패키지: 로컬 파일/SFTP 목록, UI→SSH PTY 입력, 연결 종료, 글자 크기 재로드. 콘솔 오류 없음.
- 기존 실제 Windows 탐색기 작업 7개 그룹: 복사·이동·이름 변경·폴더 생성·드래그·휴지통. 오류 없음.
- HTML 요청마다 새 스타일 nonce를 발급하며 xterm이 생성하는 스타일에만 부여. 전역 inline 스타일 허용은 사용하지 않음.

## 검증 제한

사용자의 실제 원격 서버에는 접속하지 않았습니다. 키 자동 선택은 생성한 테스트 키와 로컬 SSH 서버로 검증했습니다. 실제 Windows ssh-agent의 개인키를 이용한 원격 서명은 별도 검증이 필요합니다. MobaXterm의 X11·점프 호스트·MFA까지 구현한 것은 아닙니다.

검증 스크립트는 desktop/*.test.cjs 및 .checks/verify-*.cjs에 있으며 결과·개인 데이터는 Git에서 제외합니다.
